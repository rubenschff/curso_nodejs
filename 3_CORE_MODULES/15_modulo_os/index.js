const os = require("os")

console.log(os.cpus()) //processadores
console.log(os.freemem()) //memória livre RAM
console.log(os.homedir()) //diretorio home
console.log(os.type()) //tipo do sistema operacional